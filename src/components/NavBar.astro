<header class="nav">
	<a href="/" class="nav__brand">AI Visibility Audit</a>
	<a
		href="https://dahiana.work"
		class="nav__link"
		target="_blank"
		rel="noopener noreferrer"
	>
		Built by Dahiana ↗
	</a>
</header>

<style>
	.nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-2);
		padding: var(--space-2) var(--space-3);
		border-bottom: 1px solid var(--border);
		background: var(--surface);
	}

	.nav__brand {
		font-weight: 700;
		font-size: var(--text-small);
		color: var(--ink);
		text-decoration: none;
	}
	.nav__brand:hover {
		text-decoration: underline;
	}

	.nav__link {
		font-size: var(--text-small);
		color: var(--ink-secondary);
		text-decoration: none;
		font-weight: 500;
	}
	.nav__link:hover {
		color: var(--ink);
		text-decoration: underline;
	}
</style>
